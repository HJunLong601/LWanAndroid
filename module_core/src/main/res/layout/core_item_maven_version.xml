<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="30dp">

        <TextView
            android:id="@+id/core_item_maven_artifact_tv"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent"
            android:textColor="@color/common_text_content_color"
            android:textSize="@dimen/common_text_content_size"
            android:layout_gravity="center"
            android:gravity="center|start"
            android:layout_marginStart="@dimen/margin_normal_size"
            android:layout_marginEnd="@dimen/margin_tiny_size"
            android:maxLines="1"
            android:singleLine="true"
            android:ellipsize="end"
            tools:text="viewpager"/>

        <TextView
            android:id="@+id/core_item_maven_version_tv"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:textColor="@color/common_text_content_color"
            android:textSize="@dimen/common_text_content_size"
            android:layout_gravity="center"
            android:gravity="center"
            android:layout_marginEnd="@dimen/margin_normal_size"
            android:maxLines="1"
            android:singleLine="true"
            tools:text="28.0.0-alpha1"/>

    </LinearLayout>
</layout>